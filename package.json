{
    "name": "bbtag",
    "version": "1.0.0",
    "description": "The engine behind blargbots custom commands",
    "main": "./dist/index.js",
    "scripts": {
        "lint": "tslint src/**/*.ts test/**/*.ts",
        "build": "rimraf ./dist && cd ./src && tsc",
        "watch": "rimraf ./dist && cd ./src && tsc -w",
        "test": "rimraf ./dist ./test/out && cd ./test && tsc && cd .. && mocha --reporter spec",
        "clean": "rimraf ./dist ./test/out"
    },
    "repository": {
        "type": "git",
        "url": "https://www.github.com/blargbot/bbtag"
    },
    "author": "blargbot",
    "license": "AGPL-3.0",
    "dependencies": {
        "moment-timezone": "^0.5.20"
    },
    "devDependencies": {
        "@types/chai": "^4.1.4",
        "@types/mocha": "^5.2.2",
        "@types/moment-timezone": "^0.5.4",
        "chai": "^4.1.2",
        "mocha": "^5.2.0",
        "rimraf": "^2.6.3",
        "tslint": "^5.18.0",
        "typescript": "^2.9.2"
    }
}
