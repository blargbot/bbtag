// tslint:disable-next-line: no-implicit-dependencies
import { expect } from 'chai';
import { SortedList } from '../../src/structures';

describe('SortedList', () => {
    it('should correctly add elements', () => {

    });
});